{
  "name": "stewardship-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:performance": "npm --prefix ../frontend run build -- --configuration production,performance --source-map=false --output-path dist/performance && playwright test --config playwright.performance.config.ts",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.11.1",
    "@playwright/test": "1.58.2"
  }
}
